        private void LoginBtn_Click(object sender, RoutedEventArgs e)
        {
            MainWindow mainWindow = new MainWindow();     // replace "MainWindow" with the name of the window
            this.Close();                                 // replace "mainWindow" with any variable name you like
            mainWindow.Show();                            // close "this" window (the current window) and Show the variable name window
        }
